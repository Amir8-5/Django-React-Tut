import Form from "../components/Form";
/**
 * A functional component that renders a simple "Register" text inside a div.
 */

export default function Register() {
    return <Form route="user/register/" method="register"/>
}